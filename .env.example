# PostgreSQL 连接串(去掉 ?schema= 参数,Prisma 用 prisma.config.ts 声明 schema)
DATABASE_URL=postgresql://waincent@localhost:5432/compact

# JWT 签名密钥(生产环境请使用足够长的随机字符串)
JWT_SECRET=change-me-to-a-long-random-string

# 上传文件存储目录(默认 ./uploads,按年分目录)
UPLOAD_DIR=./uploads
